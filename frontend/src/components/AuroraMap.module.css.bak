.wrap {
  position: relative;
  border-radius: var(--radius);
  overflow: hidden;
  border: 1px solid var(--border);
  height: 100%;
  min-height: 340px;
}
.map { width: 100%; height: 100%; background: var(--bg0); }
.ovationCanvas { opacity: 0.9; mix-blend-mode: screen; }
.legend {
  position: absolute;
  bottom: 14px;
  right: 14px;
  z-index: 500;
  display: flex;
  flex-direction: column;
  gap: 5px;
  background: rgba(5,8,16,0.75);
  border: 1px solid var(--border);
  border-radius: 8px;
  padding: 8px 10px;
}
.legendItem {
  display: flex;
  align-items: center;
  gap: 7px;
  font-family: var(--mono);
  font-size: 10px;
  color: var(--text-muted);
}
.legendItem span {
  width: 8px;
  height: 8px;
  border-radius: 50%;
  display: inline-block;
  flex-shrink: 0;
}
.ovationLabel {
  position: absolute;
  bottom: 14px;
  left: 14px;
  z-index: 500;
  font-family: var(--mono);
  font-size: 10px;
  color: var(--text-dim);
  background: rgba(5,8,16,0.6);
  padding: 4px 8px;
  border-radius: 4px;
  letter-spacing: 0.06em;
}
